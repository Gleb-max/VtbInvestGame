import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '100%', 
        height: 60,
    },
    inputField: {
        flex: 1,
        alignSelf: 'stretch',
        minHeight: 47,
    },
    labelText: {
        marginBottom: 20,
    }
});

export default styles;
